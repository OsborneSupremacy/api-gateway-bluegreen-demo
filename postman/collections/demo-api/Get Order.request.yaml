$kind: http-request
name: Get Order
url: "{{domain}}/v1/order/{{CustomerId}}/{{OrderId}}"
method: GET
scripts:
  - type: afterResponse
    code: |+
      pm.test("Status code is 201", function () {
          pm.response.to.have.status(200);
      });

    language: text/javascript
order: 959152031138
